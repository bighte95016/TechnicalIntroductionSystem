# RAG系統語音輸入功能使用說明

## 功能概述
本系統已成功整合 Whisper 語音識別功能，**默認使用語音輸入模式**。用戶可以直接通過語音方式向RAG系統提問，系統會自動識別語音內容並進行智能問答。

## 新增功能
- ✅ **默認語音模式**: 系統啟動後直接進入語音輸入模式
- ✅ **語音錄製**: 使用麥克風錄製用戶語音
- ✅ **語音識別**: 使用本地 Whisper 模型進行語音轉文字
- ✅ **多語言支援**: 自動檢測中文和英文
- ✅ **實時音量顯示**: 錄音時顯示音量指示器
- ✅ **智能語言映射**: 根據識別結果選擇回答語言
- ✅ **模式切換**: 可隨時在語音和文字輸入模式間切換

## 安裝依賴項

### 1. 安裝Python包
```bash
pip install -r requirements_voice.txt
```

### 2. 系統級依賴 (根據作業系統)
- **Windows**: 通常無需額外安裝
- **Ubuntu/Debian**: 
  ```bash
  sudo apt-get install portaudio19-dev
  ```
- **macOS**: 
  ```bash
  brew install portaudio
  ```

## 使用方法

### 1. 啟動系統
```bash
python qa_RAGsystem_printChunk_QAsplit_indexTTSclone.py
```

### 2. 默認語音輸入模式 (主要使用方式)
系統啟動後自動進入語音輸入模式：
1. **直接按 Enter** 開始語音提問
2. 開始說話提問
3. 再次按 Enter 停止錄音
4. 系統自動識別語音並回答

### 3. 特殊指令
在語音模式下，您可以輸入以下指令：
- **按 Enter**: 開始語音錄音
- **text** 或 **t**: 切換到文字輸入模式
- **quit** 或 **exit**: 退出程式
- **debug**: 切換調試模式
- **help**: 查看優化建議

### 4. 文字輸入模式 (備選方式)
如需使用文字輸入：
1. 在語音模式下輸入 `text` 或 `t`
2. 切換到文字輸入模式
3. 直接輸入問題文字
4. 輸入 `voice` 或 `v` 切換回語音模式

## 語音輸入流程示例
```
=== 語音輸入模式 ===
按 Enter 開始語音提問，或輸入指令 > [直接按Enter]

開始語音錄音...
正在錄音... (按下 Enter 停止)
[=================                                 ]  # 音量指示器
按下Enter鍵停止錄音...
[按Enter]

錄音結束，正在停止錄音並保存...
音頻已保存為 temp_recording.wav
正在使用本地Whisper模型轉換語音為文字 (語言: 自動檢測)...

語音識別結果 (檢測到的語言: zh)：「什麼是全景抬頭式顯示器？」
檢測到的語言: zh (中文)
正在處理您的問題 (使用 OLLAMA LLM)...

答案：
[系統回答...]

=== 語音輸入模式 ===
按 Enter 開始語音提問，或輸入指令 > [繼續按Enter進行下一個語音提問]
```

## 模式切換示例
```
=== 語音輸入模式 ===
按 Enter 開始語音提問，或輸入指令 > text

已切換到文字輸入模式

=== 文字輸入模式 ===
請輸入您的問題 (或輸入 'voice'/'v' 切換回語音模式) > 什麼是HUD？

[系統處理文字問題...]

=== 文字輸入模式 ===
請輸入您的問題 (或輸入 'voice'/'v' 切換回語音模式) > voice

已切換到語音輸入模式

=== 語音輸入模式 ===
按 Enter 開始語音提問，或輸入指令 > [按Enter開始語音]
```

## 配置選項

### 語音相關設定 (在程式頂部)
```python
# --- 語音相關設定 ---
AUDIO_FORMAT = pyaudio.paInt16    # 音頻格式
CHANNELS = 1                      # 單聲道
RATE = 16000                      # 採樣率
CHUNK = 1024                      # 音頻片段大小
SPEECH_LANGUAGE = "auto"          # 語言檢測 ("auto", "zh", "en")

# --- Whisper 模型設定 ---
WHISPER_MODEL_SIZE = "medium"     # 模型大小 ("tiny", "base", "small", "medium", "large")

# --- 語音輸入設定 ---
ENABLE_VOICE_INPUT = True         # 啟用/關閉語音輸入功能
```

### Whisper 模型選擇建議
- **tiny**: 最快，準確度較低 (~39 MB)
- **base**: 快速，準確度一般 (~74 MB)  
- **small**: 平衡選擇 (~244 MB)
- **medium**: 推薦使用 (~769 MB) - 預設
- **large**: 最高準確度，最慢 (~1550 MB)

## 故障排除

### 1. PyAudio 安裝問題
**錯誤**: `Microsoft Visual C++ 14.0 is required`
**解決方案**: 
- 安裝 Microsoft C++ Build Tools
- 或使用預編譯版本: `pip install pipwin && pipwin install pyaudio`

### 2. 音頻裝置問題
**錯誤**: `Invalid number of channels`
**解決方案**:
- 檢查麥克風是否正確連接
- 嘗試調整 CHANNELS 和 RATE 設定

### 3. Whisper 模型下載問題
**錯誤**: 模型下載失敗
**解決方案**:
- 確保網路連接正常
- 首次使用時需要下載模型檔案
- 可手動下載並放置在 `~/.cache/whisper/` 目錄

### 4. GPU 相關問題
**說明**: 
- 系統會自動檢測 CUDA 可用性
- 無 GPU 時自動切換到 CPU 模式
- CPU 模式下語音識別會較慢但仍可正常使用

## 效能優化建議

### 1. 硬體要求
- **最低**: CPU模式，8GB RAM
- **推薦**: GPU模式，16GB RAM，CUDA支援的顯卡

### 2. 模型選擇
- 快速響應：使用 "small" 或 "base" 模型
- 高準確度：使用 "medium" 或 "large" 模型

### 3. 網路環境
- 首次使用需下載 Whisper 模型
- 後續使用無需網路連接（完全本地運行）

## 特色功能

### 1. 實時音量監控
錄音時顯示動態音量指示器，方便用戶確認音頻輸入狀態。

### 2. 智能語言檢測
自動識別中英文並選擇對應的回答語言。

### 3. 完全本地化
語音識別完全在本地進行，保護隱私安全。

### 4. 無縫整合
語音輸入與原有的文字輸入功能完美結合，用戶可隨時切換。

## 使用優勢

### 1. 更自然的交互方式
- 系統默認語音模式，無需額外指令
- 直接按 Enter 即可開始提問
- 更符合自然對話習慣

### 2. 高效的操作流程
- 減少輸入步驟，提高效率
- 語音和文字模式無縫切換
- 保留所有原有功能

### 3. 智能語音處理
- 自動語言檢測和映射
- 本地化處理保護隱私
- 實時音量反饋

## 注意事項
1. **系統默認為語音輸入模式**，啟動後直接按 Enter 開始語音提問
2. 首次使用時系統會自動下載 Whisper 模型
3. 語音識別準確度受環境噪音影響
4. 建議在安靜環境下使用以獲得最佳效果
5. 支援中文和英文語音輸入
6. 語音命令 "退出"、"結束"、"exit"、"quit" 可直接結束程式
7. 可隨時通過 `text`/`t` 切換到文字輸入模式 