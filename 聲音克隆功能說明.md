# 聲音克隆功能說明

## 概述

已成功將 RAG 系統的語音輸出功能從使用預設說話人改為使用聲音克隆技術。現在系統將使用您提供的說話人音頻文件來生成具有相似聲音特徵的語音回答。

## 主要變更

### 1. 技術變更
- **語音合成引擎**: 繼續使用 Coqui TTS 的 XTTS v2 模型
- **說話人選擇**: 從固定說話人列表改為使用音頻文件進行聲音克隆
- **音頻播放**: 從 `simpleaudio` 改為 `sounddevice` 以獲得更好的兼容性
- **採樣率**: 固定為 24000 Hz (XTTS v2 標準採樣率)

### 2. 配置變更
```python
# 新的聲音克隆設定
SPEAKER_WAV_FILE = "./voice/speaker_oreo1min.wav"  # 說話人音頻文件路徑
TTS_TEMPERATURE = 0.7  # 語氣隨機度控制 (0.0-1.0)
TTS_SAMPLE_RATE = 24000  # XTTS v2 標準採樣率

# 移除的舊設定
# FIXED_ZH_SPEAKER = "Tammie Ema"  # 不再使用固定說話人
# FIXED_EN_SPEAKER = "Tammie Ema"  # 不再使用固定說話人
```

### 3. 依賴變更
```bash
# 新依賴
sounddevice>=0.4.4
soundfile>=0.10.0

# 移除的依賴
# simpleaudio>=1.0.4  # 不再使用
```

## 文件說明

### 主要文件
- `qa_RAGsystem_printChunk_QAsplit_coquiTTSclone.py`: 已修改的主 RAG 系統文件
- `voice/speaker_oreo1min.wav`: 用於聲音克隆的說話人音頻文件
- `test_voice_clone.py`: 聲音克隆功能測試腳本

### 配置文件
- `requirements_voice.txt`: 已更新的依賴列表

## 使用方法

### 1. 安裝依賴
```bash
pip install -r requirements_voice.txt
```

或手動安裝新依賴：
```bash
pip install sounddevice soundfile
```

### 2. 測試聲音克隆功能
```bash
python test_voice_clone.py
```

### 3. 運行 RAG 系統
```bash
python qa_RAGsystem_printChunk_QAsplit_coquiTTSclone.py
```

## 聲音克隆配置

### 更換說話人音頻文件
1. 將您的音頻文件放在 `voice/` 目錄下
2. 修改 `SPEAKER_WAV_FILE` 參數：
   ```python
   SPEAKER_WAV_FILE = "./voice/your_speaker_file.wav"
   ```

### 音頻文件要求
- **格式**: WAV 格式推薦
- **時長**: 建議 30 秒到 2 分鐘
- **質量**: 清晰的單人語音，背景噪音較少
- **內容**: 最好包含豐富的語音特徵和情感變化

### 調整參數
```python
# 語氣隨機度控制
TTS_TEMPERATURE = 0.7  # 範圍 0.0-1.0
# 0.0: 更穩定、一致的語音
# 1.0: 更多變化、表現力更豐富的語音
```

## 功能特色

### 1. 自動語言檢測
系統會根據 Whisper 語音識別的結果自動選擇合適的語言進行TTS合成：
- 中文問題 → 中文回答
- 英文問題 → 英文回答

### 2. 音頻緩存
系統會緩存生成的音頻以提高響應速度：
```python
# 緩存鍵格式: (文本, 語言, 溫度)
TTS_CACHE = {}
```

### 3. 提示音功能
在處理用戶問題時播放友好的提示音：
- 中文提示: "感謝您的提問，我思考一下請稍候！"
- 英文提示: "Thank you for your question. Let me think about it for a moment."

## 疑難排解

### 1. 音頻播放問題
如果遇到 `sounddevice` 播放問題：
```bash
# 檢查可用音頻設備
python -c "import sounddevice as sd; print(sd.query_devices())"
```

### 2. 模型載入問題
如果 XTTS v2 模型載入失敗：
```bash
# 清除模型緩存重新下載
rm -rf ~/.local/share/tts/
```

### 3. GPU 支援
確保 CUDA 環境正確配置以獲得最佳性能：
```python
TTS_ENABLE_GPU = True  # 在配置中啟用 GPU
```

### 4. 音頻文件問題
如果說話人音頻文件無法使用：
- 檢查文件路徑是否正確
- 確認文件格式為 WAV
- 嘗試使用不同的音頻文件

## 性能優化

### 1. 首次使用
首次載入模型和生成音頻需要較長時間：
- 模型下載: 約 1-2 GB
- 首次生成: 比後續生成慢 2-3 倍

### 2. 後續使用
- 啟用音頻緩存提高重複文本的響應速度
- GPU 加速可顯著提升生成速度
- 預熱功能減少首次生成延遲

## 技術細節

### XTTS v2 模型特點
- **多語言支援**: 支持中文、英文等多種語言
- **零樣本克隆**: 僅需一個音頻文件即可克隆聲音
- **高質量輸出**: 24kHz 採樣率，自然流暢的語音
- **實時生成**: 優化的推理速度，適合互動應用

### PyTorch 兼容性
已解決 PyTorch >= 2.6 版本的兼容性問題：
```python
# 自動添加安全全局類
safe_classes = [XttsConfig, XttsAudioConfig, BaseDatasetConfig, XttsArgs]
torch.serialization.add_safe_globals(safe_classes)
```

## 下一步建議

1. **測試不同說話人**: 嘗試使用不同的音頻文件來找到最適合的聲音
2. **調整參數**: 根據偏好調整 `TTS_TEMPERATURE` 參數
3. **優化體驗**: 根據使用情況調整緩存策略和GPU設定
4. **擴展功能**: 考慮添加多個說話人選項或情感控制功能 