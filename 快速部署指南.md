# RAG 系統快速部署指南

## 🚀 一鍵部署（推薦）

### Windows 用戶
1. **安裝 Docker Desktop**
   - 下載：https://www.docker.com/products/docker-desktop/
   - 安裝完成後重啟電腦

2. **準備部署包**
   - 雙擊執行 `package-for-deployment.bat` 創建部署包
   - 將生成的 `rag-system-deploy` 資料夾複製到目標電腦

3. **一鍵啟動**
   - 在目標電腦上雙擊執行 `start-windows.bat`（Windows 優化版）
   - 或執行 `start.bat`（通用版）
   - 按照螢幕提示選擇模型並完成安裝

4. **開始使用**
   ```bash
   docker compose exec rag-app python roboticBar_printChunk_QAsplit_indexTTSclone_JP.py
   ```

## ⚙️ 系統需求

| 項目 | 最低配置 | 推薦配置 |
|------|----------|----------|
| **CPU** | 4 核心 | 8 核心+ |
| **記憶體** | 8GB | 16GB+ |
| **存儲空間** | 20GB | 50GB+ |
| **作業系統** | Windows 10/11 (64位) | Windows 11 |

## 📁 部署包內容

```
rag-system-deploy/
├── 🐳 Docker 配置
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── docker-compose-windows.yml (Windows 優化)
│   └── requirements.txt
├── 🚀 啟動腳本
│   ├── start.bat (通用版)
│   ├── start-windows.bat (Windows 優化)
│   └── start.sh (Linux/Mac)
├── 📚 數據目錄
│   ├── bar_file/PDF/ (放置 PDF 知識庫)
│   ├── voice/ (語音參考文件)
│   └── index_tts/ (TTS 模型)
├── 📖 說明文件
│   ├── README.txt (快速開始)
│   └── Docker部署說明.md (詳細指南)
└── ⚙️ 配置文件
    └── .env (環境變數)
```

## 🎯 模型選擇建議

| 配置等級 | 推薦模型 | 記憶體需求 | 性能表現 |
|----------|----------|------------|----------|
| **高配置** | gemma3:12b | 16GB+ | 最佳品質 |
| **中配置** | qwen2.5:7b | 8GB+ | 良好平衡 ⭐ |
| **低配置** | qwen2.5:3b | 4GB+ | 基本可用 |

> 💡 **提示**：初次使用建議選擇 `qwen2.5:7b`，在性能和資源消耗間取得良好平衡。

## 🔧 常見問題快速解決

### ❓ Docker 啟動失敗
```bash
# 重啟 Docker Desktop
# 確認 Windows 虛擬化已啟用
```

### ❓ 模型下載失敗
```bash
# 手動重新下載
docker exec ollama ollama pull mxbai-embed-large
docker exec ollama ollama pull qwen2.5:7b
```

### ❓ 音頻功能不可用
- Windows Docker 容器中音頻功能受限
- 建議使用文字模式：在代碼中設置 `ENABLE_VOICE_INPUT = False`

### ❓ 記憶體不足
1. 在 Docker Desktop 設定中增加記憶體分配
2. 選擇較小的模型（如 qwen2.5:3b）
3. 關閉其他不必要的應用程序

## 📊 系統管理命令

| 功能 | 命令 |
|------|------|
| **查看狀態** | `docker compose ps` |
| **查看日誌** | `docker compose logs -f rag-app` |
| **停止系統** | `docker compose down` |
| **重啟系統** | `docker compose restart` |
| **完全清理** | `docker compose down -v && docker system prune -f` |

## 🔄 更新系統

```bash
# 停止現有服務
docker compose down

# 重新構建鏡像
docker compose build --no-cache

# 啟動更新後的服務
docker compose up -d
```

## ⚠️ 重要提醒

1. **首次啟動**：需要下載 2-4GB 的 AI 模型，請確保網路穩定
2. **模型配置**：如選擇非 gemma3:12b 模型，需修改代碼第 59 行的 `OLLAMA_MODEL` 配置
3. **數據安全**：所有處理均在本地進行，無數據外傳風險
4. **資源監控**：建議監控系統資源使用情況，適時調整配置

## 🎉 部署完成！

部署成功後，您將擁有一個功能完整的 RAG 語音問答系統，支援：
- 📄 PDF 知識庫問答
- 🎙️ 語音輸入/輸出（可選）
- 🌐 多語言支援（中文/英文/日文）
- 🔍 混合檢索（語義+關鍵詞）
- 🎯 高精度問答匹配

如需詳細配置說明，請參考 `Docker部署說明.md`。 