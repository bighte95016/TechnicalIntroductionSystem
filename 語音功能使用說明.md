# RAG系統語音功能使用說明

## 功能概述

本RAG系統現在支持完整的語音交互功能：
- 🎙️ **語音輸入**：使用 Whisper 進行語音識別
- 🔊 **語音輸出**：使用 CoquiTTS 進行語音合成
- 🎵 **提示音功能**：在用戶提問後播放友好的提示音
- 🌐 **多語言支持**：自動檢測語言並相應回答

## 安裝依賴

### 方法一：使用 requirements 文件（推薦）
```bash
pip install -r requirements_voice.txt
```

### 方法二：手動安裝核心依賴
```bash
# 語音輸入功能
pip install pyaudio openai-whisper torch numpy

# 語音輸出功能
pip install TTS pydub simpleaudio

# 中文處理
pip install jieba
```

### Windows 系統額外注意事項
如果在 Windows 上遇到 pyaudio 安裝問題：
```bash
# 使用預編譯的 wheel 文件
pip install pipwin
pipwin install pyaudio
```

## 功能配置

在 `qa_RAGsystem_printChunk_QAsplit_coquiTTS.py` 中可以調整以下設置：

### 語音輸入設置
```python
ENABLE_VOICE_INPUT = True          # 啟用語音輸入
WHISPER_MODEL_SIZE = "medium"      # Whisper模型大小
SPEECH_LANGUAGE = "auto"           # 語言檢測模式
```

### 語音輸出設置
```python
ENABLE_VOICE_OUTPUT = True         # 啟用語音輸出
ENABLE_PROMPT_AUDIO = True         # 啟用提示音功能
COQUI_TTS_MODEL_NAME = "tts_models/multilingual/multi-dataset/xtts_v2"
TTS_ENABLE_GPU = True              # GPU加速
TTS_ENABLE_CACHE = True            # 音頻緩存
FIXED_ZH_SPEAKER = "Tammie Ema"    # 中文說話人
FIXED_EN_SPEAKER = "Tammie Ema"    # 英文說話人
```

## 使用方法

### 基本操作流程

1. **啟動系統**
   ```bash
   python qa_RAGsystem_printChunk_QAsplit_coquiTTS.py
   ```

2. **語音交互模式（默認）**
   - 🎤 按 Enter 開始錄音
   - 🗣️ 說出您的問題
   - ⏹️ 再按 Enter 停止錄音
   - 🎵 系統播放友好提示音（表示正在思考）
   - 🤖 系統自動識別語音並回答
   - 🔊 系統自動播放語音答案

3. **切換到文字模式**
   - 輸入 `text` 或 `t` 切換到文字輸入
   - 輸入 `voice` 或 `v` 切換回語音模式

### 特殊指令

- `quit` 或 `exit`：退出程式
- `debug`：切換調試模式（顯示檢索詳情）
- `help`：查看系統優化建議
- `text`/`t`：切換到文字輸入模式
- `voice`/`v`：切換到語音輸入模式

### 語音輸入技巧

1. **環境要求**
   - 確保環境相對安靜
   - 麥克風工作正常
   - 說話清晰，語速適中

2. **支持的語音退出命令**
   - 中文：「退出」、「結束」
   - 英文：「exit」、「quit」

3. **多語言支持**
   - 系統自動檢測語音語言
   - 根據檢測結果選擇相應的回答語言

## 技術特性

### Whisper 語音識別
- **模型選擇**：支持 tiny、base、small、medium、large
- **GPU 加速**：自動檢測並使用 GPU（如可用）
- **多語言**：自動語言檢測
- **高精度**：使用 OpenAI 的 Whisper 模型

### CoquiTTS 語音合成
- **模型**：使用 XTTS v2 多語言模型
- **說話人**：固定使用 "Tammie Ema" 說話人
- **緩存機制**：相同文本緩存音頻，提高響應速度
- **GPU 加速**：支持 CUDA 加速
- **高音質**：22kHz 采樣率，高品質語音

### 性能優化
- **模型預熱**：啟動時預載入模型，減少首次使用延遲
- **音頻緩存**：重複文本直接使用緩存音頻
- **並行處理**：語音生成和播放使用線程鎖管理
- **內存管理**：智能管理模型內存使用

## 故障排除

### 語音輸入問題

1. **無法錄音**
   - 檢查麥克風權限
   - 確認音頻設備可用
   - Windows 用戶檢查 Visual C++ Redistributable

2. **識別準確度低**
   - 改善錄音環境（減少噪音）
   - 說話更清晰
   - 嘗試更大的 Whisper 模型

3. **GPU 相關問題**
   - 確認 CUDA 環境正確安裝
   - 檢查 PyTorch CUDA 版本兼容性

### 語音輸出問題

1. **TTS 初始化失敗**
   - 檢查網絡連接（首次下載模型）
   - 確認磁盤空間充足
   - 檢查 TTS 庫版本

2. **音頻播放失敗**
   - 檢查音頻輸出設備
   - 確認 simpleaudio 正確安裝
   - Windows 用戶可能需要安裝音頻驅動

3. **模型下載慢**
   - XTTS v2 模型較大（約 1.8GB）
   - 首次使用需要下載，請耐心等待
   - 可考慮使用較小的模型

### 常見錯誤解決

1. **PyTorch 版本兼容性**
   ```bash
   # 如果遇到 XTTS 載入問題，嘗試降級 PyTorch
   pip install torch==2.0.1 torchaudio==2.0.2
   ```

2. **音頻設備問題**
   ```bash
   # Linux 用戶可能需要安裝額外音頻庫
   sudo apt-get install portaudio19-dev python3-pyaudio
   sudo apt-get install alsa-utils pulseaudio
   ```

3. **權限問題**
   - 確保程式有麥克風和音頻播放權限
   - macOS 用戶需要在系統設置中允許麥克風訪問

## 系統要求

### 最低配置
- Python 3.8+
- 4GB RAM
- 2GB 可用磁盤空間
- 音頻輸入/輸出設備

### 推薦配置
- Python 3.9+
- 8GB+ RAM
- NVIDIA GPU（支持 CUDA）
- 高品質麥克風和音響
- SSD 存儲

## 高級功能

### 自定義說話人
可以修改配置文件中的說話人設置：
```python
FIXED_ZH_SPEAKER = "您喜歡的中文說話人"
FIXED_EN_SPEAKER = "您喜歡的英文說話人"
```

### 模型選擇
可以嘗試不同的 TTS 模型：
```python
# 查看可用模型
tts --list_models

# 選擇其他模型
COQUI_TTS_MODEL_NAME = "tts_models/zh-CN/baker/tacotron2-DDC-GST"
```

### 性能調優
```python
# 調整緩存和GPU設置
TTS_ENABLE_CACHE = True    # 啟用緩存
TTS_ENABLE_GPU = True      # 使用GPU
TTS_SAMPLE_RATE = 22050    # 音頻品質
```

## 更新日誌

### 最新版本特性
- ✅ 完整的語音輸入輸出功能
- ✅ 多語言自動檢測和響應
- ✅ 高品質 TTS 語音合成
- ✅ 智能緩存機制
- ✅ GPU 加速支持
- ✅ 友好的用戶界面

### 已知限制
- TTS 模型首次下載需要時間
- GPU 內存使用量較大
- 某些語言的識別準確度可能有差異

## 技術支持

如果遇到問題，請檢查：
1. 依賴是否正確安裝
2. 音頻設備是否正常工作
3. 網絡連接是否穩定（用於模型下載）
4. 系統權限是否充足

享受您的語音 RAG 系統體驗！🎉 