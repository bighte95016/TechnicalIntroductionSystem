# RAG聊天機器人 Web UI 使用說明

## 🌐 概述

Web UI提供了一個基於瀏覽器的現代化界面，讓您可以通過網頁與RAG聊天機器人進行交互。支持語音輸入、文字輸入和音頻文件上傳等多種交互方式。

## ✨ 主要功能

### 🎤 語音問答
- **實時錄音**：點擊按鈕開始錄音，再次點擊停止
- **自動語音識別**：使用Whisper模型進行語音轉文字
- **多語言支持**：自動檢測中文、英文、日文
- **語音回答**：系統回答支持TTS語音播放

### 📝 文字問答
- **文字輸入**：直接在文本框中輸入問題
- **快捷鍵支持**：按Enter鍵快速提交問題
- **即時回答**：快速獲得文字和語音回答

### 📁 音頻文件上傳
- **文件支持**：支持WAV、MP3等常見音頻格式
- **批量處理**：可上傳預錄的音頻文件進行處理

### 💬 聊天界面
- **消息記錄**：完整保存問答歷史
- **語音播放**：每個回答都附帶音頻播放器
- **美觀設計**：現代化的聊天氣泡界面

## 🚀 快速開始

### 1. 安裝依賴

```bash
# 安裝Web UI額外依賴
pip install -r requirements_web.txt

# 確保已安裝主程序依賴
pip install -r requirements.txt
```

### 2. 啟動Web服務器

```bash
python web_ui.py
```

### 3. 打開瀏覽器

訪問：`http://localhost:5000`

## 📋 系統要求

### 服務器端
- Python 3.8+
- 所有RAG系統依賴項
- Flask和相關Web框架

### 客戶端（瀏覽器）
- 現代瀏覽器（Chrome、Firefox、Safari、Edge）
- 支持WebRTC（用於麥克風訪問）
- 支持WebSocket（用於實時狀態更新）

## 🎯 使用方法

### 語音問答流程

1. **等待系統就緒**
   - 頁面加載後，等待狀態指示器變為綠色
   - 看到"RAG系統已就緒！"消息

2. **開始語音提問**
   - 點擊"開始語音提問"按鈕
   - 允許瀏覽器訪問麥克風
   - 看到紅色錄音指示器

3. **說出問題**
   - 清晰地說出您的問題
   - 支持中文、英文、日文

4. **停止錄音**
   - 再次點擊按鈕停止錄音
   - 等待系統處理

5. **查看結果**
   - 問題和答案顯示在聊天區域
   - 點擊音頻播放器聽取語音回答

### 文字問答流程

1. **輸入問題**
   - 在文本框中輸入您的問題
   - 支持多行輸入

2. **提交問題**
   - 點擊"提交問題"按鈕
   - 或按Enter鍵快速提交

3. **查看回答**
   - 問題和答案立即顯示
   - 附帶語音播放功能

### 音頻文件上傳

1. **選擇文件**
   - 點擊文件上傳區域
   - 選擇音頻文件（WAV、MP3等）

2. **自動處理**
   - 文件上傳後自動開始處理
   - 語音識別和問答一次完成

## 🎨 界面說明

### 頂部狀態欄
- **綠色圓點**：系統就緒
- **橙色圓點**：系統初始化中
- **紅色圓點**：系統錯誤

### 聊天區域
- **右側氣泡**：您的問題（藍色漸變）
- **左側氣泡**：系統回答（灰色）
- **音頻播放器**：回答的語音版本
- **時間戳**：每條消息的時間

### 控制面板
- **語音問答**：錄音按鈕和狀態指示
- **文字問答**：文本輸入框和提交按鈕
- **文件上傳**：音頻文件選擇區域

## ⚙️ 高級設置

### 服務器配置

在 `web_ui.py` 中可以修改：

```python
# 服務器端口
socketio.run(app, host='0.0.0.0', port=5000)

# 修改為其他端口
socketio.run(app, host='0.0.0.0', port=8080)
```

### 音頻設置

系統會自動使用主程序的TTS設置：
- IndexTTS用於中文和英文
- pyttsx3用於日文（如果啟用）

## 🔧 故障排除

### 常見問題

**Q: 無法訪問麥克風**
A: 
- 確保瀏覽器允許麥克風訪問
- 檢查系統麥克風權限
- 嘗試使用HTTPS（某些瀏覽器要求）

**Q: 語音識別失敗**
A:
- 確保說話清晰，環境安靜
- 檢查Whisper模型是否正確載入
- 嘗試使用文字輸入作為替代

**Q: 系統初始化失敗**
A:
- 檢查PDF文件是否存在於 `./bar_file/PDF/`
- 確保所有依賴項已正確安裝
- 查看控制台錯誤信息

**Q: 語音回答無法播放**
A:
- 檢查TTS系統是否正常工作
- 確保瀏覽器支持音頻播放
- 檢查 `./voice/JP_pyttsx3_v2.wav` 文件是否存在

### 性能優化

1. **服務器性能**
   - 使用GPU加速Whisper和TTS
   - 增加服務器內存
   - 使用SSD存儲

2. **網絡優化**
   - 使用本地網絡部署
   - 優化音頻文件大小
   - 啟用gzip壓縮

3. **瀏覽器優化**
   - 使用最新版本瀏覽器
   - 關閉不必要的標籤頁
   - 清理瀏覽器緩存

## 🔒 安全考慮

### 訪問控制
- 默認綁定到所有網絡接口（0.0.0.0）
- 生產環境建議設置防火牆規則
- 考慮添加身份驗證機制

### 數據隱私
- 音頻文件在處理後自動刪除
- 聊天記錄僅存儲在瀏覽器本地
- 不會永久保存用戶語音數據

## 🌟 最佳實踐

1. **首次使用**
   - 先測試文字輸入功能
   - 確認系統回答質量
   - 再嘗試語音功能

2. **語音輸入技巧**
   - 在安靜環境中使用
   - 說話清晰，語速適中
   - 避免背景噪音

3. **系統維護**
   - 定期清理臨時文件
   - 監控服務器資源使用
   - 更新依賴項版本

## 📞 技術支持

如果遇到問題：

1. 檢查瀏覽器控制台錯誤信息
2. 查看服務器終端輸出
3. 確認所有依賴項已正確安裝
4. 嘗試重啟Web服務器

Web UI提供了更友好的用戶體驗，同時保持了命令行版本的所有核心功能！ 